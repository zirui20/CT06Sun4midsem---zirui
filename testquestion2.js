function setup() {
    createCanvas(600,600);
    background(220)
}

let colorValue = 0;
let spacing = 90;

function draw() {
    let diameter = 50;
    let numCircles = 10; 
    let totalWidth = diameter * numCircles;
    for(let i =0; i<10; i++)
        cir 
}