function setup() {
    createCanvas(600,600);
    background(220)
}

let colorValue = 0;
let spacing = 90;

function draw() {
    let diameter = 50;
    let numCircles = 10; 
    let totalWidth = diameter * numCircles;
     let startX = diameter/2; 
     let startY = diameter/2; 
}
